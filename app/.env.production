# Frontend Production Environment Variables
# These will be loaded during the build process via GitHub Actions

# Backend API URL - will be injected by GitHub Actions workflow
# If not set during build, default to railway domain placeholder
REACT_APP_API_URL=https://api.yourdomain.com

# Enable/disable features for production
REACT_APP_ENABLE_ANALYTICS=true
REACT_APP_DEBUG_MODE=false
